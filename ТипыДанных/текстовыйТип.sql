

--DECLARE @t1 text,
--		@t2 ntext,
--		@t3 varchar(MAX),
--		@t4 nvarchar(MAX)

--SELECT Len(@t1)
--SELECT Len(@t2)
--SELECT Len(@t3)
--SELECT Len(@t4)

--PRINT Len(@t3)

--ALTER TABLE styles.main
--	ADD title varchar(MAX)
USE GLOB
go

INSERT INTO styles.main(title)
VALUES ('sddsddsdsssds')

SELECT Len(title)
FROM styles.main
go




